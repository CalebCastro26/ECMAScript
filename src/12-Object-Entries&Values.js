const zoo = {
  felinos: 'Tigre',
  repetiles: 'Cocodrilo',
}

let keys = Object.keys(zoo)
let values = Object.values(zoo)
let entries = Object.entries(zoo)

console.log(keys)
console.log(values)
console.log(entries)
